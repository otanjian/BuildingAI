## 1. Regression Contract

- [x] 1.1 Add focused tests that require left/right panel glyphs and preserve the two buttons'
      existing interaction and accessibility bindings.

## 2. Icon Replacement

- [x] 2.1 Replace the shared desktop information/history toggle glyph with the left-side panel icon.
- [x] 2.2 Replace the OpenCode workspace toggle glyph with the right-side panel icon without
      changing the button props or handler.

## 3. Verification

- [x] 3.1 Run the focused client test, TypeScript check, and scoped lint/format validation.
- [x] 3.2 Browser-verify both icons and confirm that each still opens and closes its original panel.
