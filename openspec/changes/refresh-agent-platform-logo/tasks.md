## 1. Asset Contract

- [x] 1.1 Add a failing automated contract test for the default SVG's accessibility, compact geometry, AI motif, and existing sidebar fallback path.

## 2. Logo Implementation

- [x] 2.1 Replace the default Agent platform SVG with the approved AI aperture artwork while preserving its public URL and accessible label.
- [x] 2.2 Render the SVG at its native size and at the sidebar's 32 px size, then inspect contrast, clipping, and motif legibility.

## 3. Verification

- [x] 3.1 Run the focused asset contract test and shared UI test suite.
- [x] 3.2 Run the relevant frontend typecheck/build validation and OpenSpec validation.
- [x] 3.3 Verify the Agent platform page in a browser with the new default logo and confirm custom-logo precedence remains unchanged in code.
