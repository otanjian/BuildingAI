## 1. Embed shell visibility contract

- [x] 1.1 Add failing unit coverage for embedded header-action and secondary-panel visibility, including direct-route preservation
- [x] 1.2 Implement the shared pure embed-shell visibility decision beside the existing BuildingAI embed utilities

## 2. Session shell integration

- [x] 2.1 Gate review, file-tree, terminal, resize-handle, and aggregate side-panel layout state in the session page
- [x] 2.2 Gate the session timeline context/status and overflow action cluster while retaining the title
- [x] 2.3 Run focused app tests, typecheck, formatting, lint, and direct-route regression coverage

## 3. Build and deployment verification

- [x] 3.1 Validate the OpenSpec change and build the workspace single OpenCode binary with embedded web UI
- [x] 3.2 Wait for an idle managed runtime, restart OpenCode, and verify the deployed health version
- [x] 3.3 Browser-verify the representative BuildingAI embed has neither red-boxed area, uses the released width, and retains its title/content
- [x] 3.4 Record fresh verification evidence and confirm unrelated dirty working-tree changes were preserved
